{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "FILE=$(jq -r '.tool_input.file_path // .tool_response.filePath'); if echo \"$FILE\" | grep -q '^/home/claude/repo/dist/'; then cd /home/claude/repo && git add . && git diff --cached --quiet || (git commit -m \"update: auto-commit\" && git push origin main); fi 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Committing & pushing to GitHub..."
          }
        ]
      }
    ]
  }
}
